// tests relating to loading and saving config, including persistence during navigation
describe('config tests', () => {
  describe('given a user has no config', () => {
    describe('when opening the app', () => {
      test.todo('the app loads with default settings');
    });

    describe('when updating their settings', () => {
      test.todo('the app settings update');
    });
  });

  describe('given a user has set config', () => {
    describe('when opening the app', () => {
      test.todo('the app loads with their settings');
    });
  });
});
